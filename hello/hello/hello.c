/**
 * main.c
 * * Este é um programa básico de exemplo que demonstra a estrutura
 * mínima de um programa em C.
 * * Objetivo: Imprimir a mensagem "Hello, World!" no terminal.
 */

#include <stdio.h>

int main(void)
{
    // Imprime a string de saudação padrão no fluxo de saída (stdout)
    printf("Hello, World!\n");

    // Retorna 0 para indicar que o programa foi finalizado com sucesso
    return 0;
}
